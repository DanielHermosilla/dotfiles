" 1) jk para salir de insert mode
inoremap jk <Esc>
set timeout
set timeoutlen=300

" 2) Clipboard del sistema (requiere Vim con +clipboard)
set clipboard=unnamedplus
" Si tu Vim no tiene +clipboard, instala el de brew:
"   brew install vim

